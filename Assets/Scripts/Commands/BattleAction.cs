/// <summary>A menu choice offered by AwaitingConfirmState — not a state machine standing behind an enum; the actual flow is real state classes.</summary>
public enum BattleAction
{
    Wait,
    Attack,
}
